const databaseReducer = (state, action) => {
  console.log('database reducer state:', state);
  console.log('database reducer action', action);
};

module.exports = databaseReducer;

// export default databaseReducer;
