import React, { Component } from 'react';

const DatabaseComponent = (props) => {
    return (
      <div id='singleDatabase' onClick={} onHover={}>
        Database {props.databaseNum}
      </div>
    );

}

export default DatabaseComponent;